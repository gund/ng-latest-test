export const API_TYPE = 'json';

/**
 * API Endpoints in format:
 * {
 *   endpoint: {
 *     toString() {
 *       return '/endpoint';
 *     }
 *   }
 * }
 */
export const API_URLS = {
  test: '123'
};
